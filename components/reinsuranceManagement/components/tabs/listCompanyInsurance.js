import React, {useState} from "react";
import {useTranslation} from "react-i18next";

function ListCompanyInsurance(props) {
    const {t} = useTranslation('common');

    return (
        <>Danh sách Công ty bảo hiểm gốc</>
    );
}

export default ListCompanyInsurance;